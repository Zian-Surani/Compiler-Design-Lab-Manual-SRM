# Lab 11 – Canonical LR(1) Parser

## Objective

To construct a **Canonical LR(1) parsing table** using **lookahead symbols**.

## Features

* LR(1) item generation
* Lookahead-aware closure computation
* ACTION/GOTO parsing tables
* Deterministic parsing simulation

## Concepts Used

* LR(1) parsing theory
* Deterministic context-free recognition
* Full power bottom-up parsing

## How to Run

```bash
gcc lab11.c -o lab11
./lab11
```
